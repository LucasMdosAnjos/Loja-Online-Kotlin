<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LojaOnline"
        tools:targetApi="31">
        <activity
            android:name=".pages.home.HomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LojaOnline">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pages.login.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.LojaOnline" />
        <activity
            android:name=".pages.register.RegisterActivity"
            android:exported="false"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.LojaOnline" />
        <activity
            android:name=".pages.welcome.WelcomeActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.LojaOnline" />
        <activity
            android:name=".pages.addProduto.AddProdutoActivity"
            android:exported="false"
            android:label="@string/title_activity_addProduto"
            android:theme="@style/Theme.LojaOnline" />
        <activity
            android:name=".pages.carrinho.CarrinhoActivity"
            android:exported="false"
            android:label="@string/title_activity_carrinho"
            android:theme="@style/Theme.LojaOnline" />
    </application>

</manifest>